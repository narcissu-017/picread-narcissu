# PicRead

[![English](https://img.shields.io/badge/README-English-2d6cdf?style=for-the-badge)](README.md) [![简体中文](https://img.shields.io/badge/%E8%AF%B4%E6%98%8E-%E7%AE%80%E4%BD%93%E4%B8%AD%E6%96%87-2d6cdf?style=for-the-badge)](README.zh-CN.md)

PicRead 是一个用于批量审阅图片和 GIF 的多窗口平铺工具。

它适合这些场景：
- 同时查看大量图片/GIF
- 在尽量不浪费桌面空间的前提下对比素材
- 保持图片原始顺序和宽高比进行审阅
- 为不同题材、项目或批次保存模板，重复打开使用
- 其实最大的作用是方便你用你巨大的屏幕，自动平铺各种你精选的图片、动图即刻起飞

## 本版本主要功能
- 多窗口组同时打开
- 支持 PNG、JPG、JPEG、BMP、WEBP、GIF
- GIF 播放与静态图混合查看
- 保持宽高比自动平铺
- 固定行数排版与智能排版
- 组内拖动调整顺序
- 多选后批量移除图片
- 模板库、历史记录、会话保存
- 模板打开、更新、重新加载、删除
- 合并窗口组
- 拖拽导入图片
- 独立性能模式与调参面板

## **2026-04-09 · 0.85 更新说明**
- 调整了窗口组页和模板库页的顶部工具栏排布。
- 加入了中英文切换，并补全了主要界面的英文文案。
- 为大量 GIF 使用场景加入了负载档位。
- 优化了窗口缩放时的缓存处理，减少缓存持续堆积带来的卡顿。

## **2026-04-15 · 0.87 更新说明**
- 继续优化了大量 GIF 场景下的窗口缩放与交互响应，窗口调整和整理素材时更稳定。
- 改进了模板保存和更新时的弹窗显示逻辑，减少弹窗被遮挡后看起来像“卡死”的情况。
- 现在再次点击已选中的图片，可以直接取消选中，减少选中框对审阅的遮挡。
- 图片窗口右键新增“重播本窗口所有 GIF”，方便把当前窗口内的动图统一从头开始重新对比。

## 下载与使用
仓库内已附带可直接运行的发布版：
- `dist/PicRead.exe`

使用说明：
- 直接运行 EXE 不需要提前安装 Python
- 当前 one-file 打包版本已包含界面语言文件和图标资源，单独使用 `PicRead.exe` 也可以正常运行
- 首次运行时，程序会在 `dist/state/` 下自动生成自己的本地状态目录
- 如果你把 EXE 移动到别的位置，建议把整个 `dist` 目录一起移动，避免状态文件分散
- **2026-04-09 · 播放提示：** 同时打开大量 GIF，或者调整窗口组大小时，程序可能会短暂停顿以重建缓存。缓存稳定后一般会恢复流畅播放；如果仍然卡顿，可以尝试提高负载档位，以获得更积极的缓存策略和更高的更新预算。

## 从源码运行
环境要求：
- Windows 10/11
- Python 3.12 或相近版本

安装依赖：
```powershell
python -m pip install -r requirements.txt
```

启动：
```powershell
python app.py
```

也可以双击：
- `start_picread.bat`

## 基本使用
1. 启动程序后先创建窗口组。
2. 拖拽图片或 GIF 到窗口组中，或使用界面按钮导入。
3. 根据需要切换固定排版或智能排版。
4. 在窗口组中拖动图片即可调整顺序。
5. 右键或按 `Delete` 可从当前窗口组移除图片。
6. 模板库可用于保存和重复打开常用图片组。
7. 历史记录可快速恢复之前的查看状态。

## 审阅辅助功能
- 负载档位适合按机器性能和 GIF 数量灵活切换。
- 再次点击已选中的图片，可直接取消选中状态。
- 在图片窗口右键，可快速重播当前窗口内的全部 GIF。
- 模板支持保存、更新、重新加载，方便反复整理后继续使用。

## 模板与历史
- 模板：适合保存常用图片组合、顺序和排版设置。
- 历史：适合恢复最近使用状态。
- 这些内容默认写到本地 `state/` 目录，不会影响原始图片文件。

## 算法说明
本仓库附带了独立的排版算法说明文档：
- `docs/layout_algorithms.md`

里面说明了当前主要排版算法的设计目标、核心思路和关键代码片段，方便继续迭代和交流。

## 想请教和征集建议
目前智能排版已经能满足日常使用，但我相信还有继续优化的空间，尤其是在：
- 空间利用率
- 横竖图混排效果
- 大量图片/GIF 时的稳定性
- 不同窗口尺寸下的布局自然度

如果有大佬愿意帮忙一起看看、更进一步优化算法，非常欢迎交流和建议。
